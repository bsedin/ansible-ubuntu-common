---
- include: apt.yml

- include: hostname.yml
  when: hostname is defined

- include: timezone.yml
  when: timezone is defined

- include: admin_users.yml
  when: admin_users is defined

- include: ssh.yml

- include: fail2ban.yml
  when: ubuntu_common_setup_fail2ban

- include: cpufreq.yml
  when: ubuntu_common_cpufreq_performance
